[build]
target = "avr-none"
rustflags = ["-C", "target-cpu=atmega328p"]

[target.'cfg(target_arch = "avr")']
runner = "ravedude"

[unstable]
build-std = ["core"]

[alias]
tank-build = "build --release --bin tank-plant"
tank-run = "run --release --bin tank-plant"
